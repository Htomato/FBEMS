<view class="Container">
    <view>
        <!-- show 的值绑定到提交按钮的返回值。 -->
        <mp-toptips msg="{{success}}" type="{{topStatus}}" show="{{show}}"></mp-toptips>
    </view>
    <view class="formContainer">
        <form bindsubmit="formSubmit" bindreset="formReset">
            <view class="formItems">
                <view>
                    <text>姓名</text>
                    <input type="text" name="driverName" placeholder="例如：张三"></input>
                </view>
                <view class="ipt-wrap flex-row ai-center">
                    <text class="font14 mr40">驾照类型:</text>
                    <radio-group bindchange="licenseTypeRadio" class="flex-vertical-center">
                        <radio class="ml5" wx:for-items="{{licenseTypeRadio}}" wx:key="name" value="{{item.name}}">
                            <view class="as-center">{{item.value}}</view>
                        </radio>
                    </radio-group>
                </view>
                <view>
                    <text>队别</text>
                    <input type="text" name="driverCompany" placeholder="例如：消防一队"></input>
                </view>
            </view>
            <view class="button">
                <button form-type="reset" style="background-color: #939393">清除</button>
                <button form-type="submit" style="background-color: #8DE">查询</button>
            </view>
        </form>
    </view>
    <view class="table">
        <view class="tr bg-w">
            <view class="th">申请人</view>
            <view class="th">申请状态</view>
            <view class="th">牌照类型</view>
            <view class="th">申请时间</view>
            <view class="th">上级单位</view>
        </view>
        <block wx:for="{{applyList}}" wx:key="index">
            <view class="tr bg-g" wx:if="{{index % 2 == 0}}">
                <view class="td ">{{item.driverName}}</view>
                <view class="td color-0cf"  data-id="{{item.driverId}}"  bindtap="changeApply">待审核</view>
                <view class="td">{{item.licenseType}}</view>
                <view class="td">{{item.driverApplydate}}</view>
                <view class="td" >{{item.driverCompany}}</view>

            </view>
            <view class="tr" wx:else>
                <view class="td">{{item.driverName}}</view>
                <view class="td color-0cf"  data-id="{{item.driverId}}"  bindtap="changeApply">待审核</view>
                <view class="td">{{item.licenseType}}</view>
                <view class="td">{{item.driverApplydate}}</view>
                <view class="td" >{{item.driverCompany}}</view>
            </view>
        </block>
    </view>
</view>
